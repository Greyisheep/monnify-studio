# Backend origin for live IR + analyzer (optional - fixtures work offline)
NEXT_PUBLIC_API_URL=http://127.0.0.1:8010

# Failure-trace / chaos report UI scaffold (#29). Requires #11 scenario engine for full flow.
NEXT_PUBLIC_CHAOS_UI=false
